- [x] `views/v1` 整体视觉与原版 xspace 风格保持一致，不再形成独立子主题
- [x] `ViewXspace.vue`、`TopBar.vue`、`DesktopIcon.vue`、`Dock.vue`、窗口组件之间的材质、圆角、留白和层级保持统一
- [x] v1 中主要颜色、背景、边框、阴影和文本层级优先使用 `entry.vue` 体系提供的全局 CSS 变量
- [x] 组件内不存在成片散落的硬编码主题主色，必要硬编码仅限极少数无法变量化的装饰场景
- [x] 按钮、图标、输入框、对话框、表单项、空态等元素优先复用项目现有组件或现有组件包装层
- [x] 未为桌面版重复实现本可由 `ui-x` 或现有业务组件承担的基础控件
- [x] `ApiManager.vue`、`Explore.vue` 等模块内容区的控件表达尽量贴近原版业务页面，而不是重新设计一套桌面版控件体系
- [x] 新增样式仅作为对现有主题变量和组件能力的补充，不破坏当前功能逻辑
- [x] 修改范围聚焦 `static/business_xspace/views/v1`，不为样式对齐额外扩散到无关模块
- [x] 最终实现符合项目现有前端规范，包括动态加载架构、Less 组织方式和组件复用原则
