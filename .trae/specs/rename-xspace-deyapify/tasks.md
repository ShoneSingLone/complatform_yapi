# Tasks
- [ ] Task 1: 脚本方案细化与确认（生成脚本前必须完成）
  - [ ] 梳理用户可见的品牌触点（标题、菜单、页面标题、弹窗、Toast、空状态、登录页等）
  - [ ] 梳理工程命名触点（文件/目录名、路由名、API 命名空间、本地存储 key、全局变量）
  - [ ] 产出迁移策略：默认严格去 xspace；可选兼容模式（别名/跳转/一次性迁移）
  - [ ] 明确脚本默认排除与允许修改范围（至少排除 .git/node_modules/static/common/ 构建产物）
  - [ ] 明确替换词表与文件重命名规则（大小写/边界/仅单词命中 vs 子串命中）

- [ ] Task 2: 实现 Node.js 迁移脚本（dry-run 默认）
  - [ ] `scan`：统计命中与候选变更（不写入）
  - [ ] `apply`：在显式 `--apply` 下执行替换/重命名（写入）
  - [ ] `verify`：复验（检索残留、输出允许残留列表）
  - [ ] 生成详细使用说明与示例命令（每一步输出预期与风险提示）

- [ ] Task 3: 前端品牌去 xspace 化（用户可见层）
  - [ ] 替换应用标题与入口页面的展示名称为 XSpace
  - [ ] 替换菜单/导航/页面标题/提示语等文本，确保不出现 “XSpace/xspace”
  - [ ] 替换 logo / favicon 等品牌资产引用（如存在）

- [ ] Task 4: 入口与路由命名调整
  - [ ] 新增或调整 XSpace 入口（例如 xspace.html / 或现有入口改名）
  - [ ] 调整路由前缀/路由命名（如存在 xspace 前缀），并同步菜单链接
  - [ ] 处理旧入口与旧路由：严格模式下 404/跳转；兼容模式下静默跳转到新入口

- [ ] Task 5: API 命名空间迁移（工程层去 xspace 化）
  - [ ] 在 API 封装层新增 `_api.xspace`（或等价命名空间）并迁移调用点
  - [ ] 全量替换 `_api.xspace.*` 引用为 `_api.xspace.*`
  - [ ] （可选）兼容模式：保留 `_api.xspace` 作为别名，确保旧调用仍可工作

- [ ] Task 6: 本地持久化与全局命名迁移
  - [ ] 替换 LocalStorage/Cookie/事件名/全局变量等 `xspace` 前缀为 `xspace`
  - [ ] （可选）兼容模式：实现旧 key -> 新 key 的一次性迁移读取
  - [ ] 验证用户本地状态的影响（例如登录态、上次选择项目等）

- [ ] Task 7: 文档与仓库元信息更新
  - [ ] 更新仓库自有文档中 “XSpace/xspace” 命名为 “XSpace/xspace”
  - [ ] 若存在可见的版权/第三方声明，保持不修改

- [ ] Task 8: 验证与回归
  - [ ] 通过全文检索确认用户可见层无 “XSpace/xspace”
  - [ ] 验证关键路径：登录、项目切换、接口查看/编辑、保存、上传
  - [ ] 验证旧入口/旧路由与兼容策略行为符合 spec

# Task Dependencies
- Task 2 依赖 Task 1（脚本方案需先确认）
- Task 3/4/5/6 由脚本驱动批量修改，但对外验收依赖 Task 8
- Task 7 可与 Task 3 并行
