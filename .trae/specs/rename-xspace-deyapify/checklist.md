- [ ] 迁移通过 Node.js 脚本完成：默认 dry-run，不带显式开关不会修改文件；输出变更清单与统计
- [ ] 脚本具备路径过滤与默认排除（至少排除 .git/node_modules/static/common/ 构建产物），避免误改第三方库
- [ ] 前端所有用户可见区域（标题、菜单、页面标题、弹窗、Toast、空状态）不出现 “YApi/yapi”
- [ ] 品牌资产（logo/favicon/应用名展示）已替换为 XSpace（如项目存在这些资产）
- [ ] 新入口/新路由以 XSpace 命名可正常访问，且菜单链接一致
- [ ] 旧入口/旧路由行为符合策略：严格模式 404/跳转；兼容模式静默跳转
- [ ] `_api.yapi.*` 调用已迁移为 `_api.xspace.*`（严格模式下代码中不再引用 `_api.yapi`）
- [ ] 本地持久化 key 前缀已迁移为 `xspace`（兼容模式下旧 key 可一次性迁移）
- [ ] 全文检索 “YApi/yapi” 仅剩允许位置（第三方版权信息、历史兼容层或后端不可见路径）
- [ ] 关键回归路径可用：登录、项目切换、接口查看/编辑、保存、上传
