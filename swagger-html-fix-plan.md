# swagger.html 访问异常修复计划

## 背景
用户反馈 `swagger.html` 当前无法访问，需要排查访问链路并完成修复。

## 初步计划
1. 定位 `swagger.html`、`openapi.html`、相关静态资源与服务端路由的映射关系。
2. 排查页面不可访问的根因（文件缺失、路由未注册、静态资源未暴露、重写冲突等）。
3. 在最小影响范围内完成修复，并检查是否影响文档页面的其他入口。
4. 通过本地验证确认修复结果，并整理结论。
